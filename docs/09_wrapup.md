# 09. まとめ

## 設計判断の比較表

| 観点 | Cookieセッション | 素朴なJWT(2〜4章) | JWT+リフレッシュ(5章) |
|---|---|---|---|
| サーバの状態 | 必要 | 不要 | アクセストークンは不要、リフレッシュのみ必要 |
| 即時失効 | 簡単 | 不可能 | リフレッシュを消せば実質可能(アクセスは期限まで有効) |
| 検証コスト | ストア参照 | 署名検証のみ(高速) | 同左 |
| 最大のリスク | CSRF、セッションID漏洩 | 検証ロジックの実装ミス(5〜7章) | 検証ロジック + 鍵管理の運用 |

「JWTだからセキュア」でも「セッションだからレガシー」でもない。**何を状態としてサーバに残し、何をトークンに埋め込むか**というトレードオフの選択であり、5章のサーバは実質「短命JWT + サーバ側リフレッシュセッション」というハイブリッドに落ち着いている。

## 本番投入前のチェックリスト

- 署名アルゴリズムはサーバ側で固定しているか(`alg`ヘッダを信用していないか)
- 鍵は十分な長さの乱数か、辞書に載っていないか
- 公開鍵(RS256等)を別の用途の鍵として使い回していないか
- アクセストークンの有効期限は短いか、失効の手段があるか
- トークンに機密情報(パスワード、カード番号等)を入れていないか(payloadは平文)
- CookieはHttpOnly / SameSite / Secure(本番はHTTPS前提)が付いているか
- 権限判定(RBAC)は、検証済みのクレームだけを根拠にしているか

## この先

- RFC 7519 (JSON Web Token)
- RFC 6265 (HTTP State Management Mechanism, Cookie)
- OWASP JWT Cheat Sheet
- OWASP API Security Top 10 (特にBroken Object Level Authorization — 今回`/admin`で見た「クレームだけを信用する」設計の延長線上にある問題)
